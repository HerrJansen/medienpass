import App from "./App.js";

const rootElement = document.getElementById("root");
rootElement.innerHTML = "";
rootElement.appendChild(document.createTextNode("App konnte noch nicht gerendert werden."));
